// app contstant like APIbase URL, API keys, etc.
import 'package:flutter/material.dart';

class AppConstant {
  // Base URL for the API
  static const String apiBaseUrl = 'http://127.0.0.1:3000/api/v1';
 //http://127.0.0.1:3000 
  // Default padding for UI elements
  static const double defaultPadding = 16.0;

  // Default border radius for UI elements
  static const BorderRadius defaultBorderRadius = BorderRadius.all(Radius.circular(8.0));

  // Default text style for headings

}